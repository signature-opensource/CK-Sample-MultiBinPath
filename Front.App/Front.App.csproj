<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <!-- Since we have to reference "Web" packages, we must target NetCoreApp 3.1. -->
    <TargetFramework>net6.0</TargetFramework>
    <!-- This forces MSBuild to generate the runtimeconfig.json file with the Microsoft.AspNetCore.App reference. -->
    <GenerateRuntimeConfigurationFiles>true</GenerateRuntimeConfigurationFiles>
    <IsPackable>false</IsPackable>
  </PropertyGroup>
  <ItemGroup>
    <!-- This reference the AspNet Core meta package. -->
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
    <PackageReference Include="CK.DB.AspNet.Auth" Version="0.0.0--03n5axf-local+v16.0.0-r01" />
    <PackageReference Include="CK.DB.User.UserOidc" Version="0.0.0--03n58ps-local+v14.0.0-r01" />
  </ItemGroup>
</Project>